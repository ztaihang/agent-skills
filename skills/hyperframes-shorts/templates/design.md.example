---
name: Data Drift
colors:
  primary: "#0a0a0a"
  on-primary: "#e0e0e0"
  accent-purple: "#7c3aed"
  accent-cyan: "#06b6d4"
typography:
  headline:
    fontFamily: Inter
    fontSize: 2.5rem
    fontWeight: 200
    letterSpacing: 0.05em
  body:
    fontFamily: Inter
    fontSize: 0.875rem
    fontWeight: 300
rounded:
  sm: 4px
  md: 12px
  full: 9999px
spacing:
  sm: 16px
  md: 32px
  lg: 64px
motion:
  energy: moderate
  easing:
    entry: "sine.inOut"
    exit: "power2.out"
    ambient: "sine.inOut"
  duration:
    entrance: 1.0
    hold: 2.5
    transition: 1.5
  atmosphere:
    - particle-field
    - light-traces
    - radial-glow
  transition: gravitational-lens
---

# Overview

（1 段：本视频视觉气质、与口播内容的匹配理由。Agent 按所选 visual-styles.md 预设改写，勿留本示例原文。）

## Colors

- 背景 `{colors.primary}`，前景 `{colors.on-primary}`
- 强调色 `{colors.accent-purple}` / `{colors.accent-cyan}` 分工：数据用 cyan，关键词用 purple
- 禁止额外发明第四套 accent

## Typography

- 标题 `{typography.headline}` — 竖屏 1080 宽时 headline 不小于 48px 等效
- 正文/字幕参考 **web-typography** skill 移动端层级
- **底部字幕 `.sl`**：`white-space: nowrap`；横屏单条 ≤16 汉字，竖屏 ≤12 汉字，超出拆 `lines.json`
- 禁止 Inter + 渐变字 作为唯一识别（lazy default）

## Motion

- 入场遵循 **design-motion-principles**：stagger 0.08–0.15s，禁止每元素同一 `power2.out`
- 背景装饰遵循 **css-animations**：至少 2 层 atmosphere 慢速 ambient 循环
- 转场：`{motion.transition}`

## Do's and Don'ts

- ✅ 每镜 2–5 个装饰层，共享一种 ambient 呼吸
- ✅ 数据镜用 `{colors.accent-cyan}` 高亮数字
- ❌ 左侧 accent 竖条、纯黑 `#000`、未声明的 neon cyan
