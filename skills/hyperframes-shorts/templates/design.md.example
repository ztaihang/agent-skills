---
name: Data Drift
styleName: Data Drift
openingLayout: kinetic-typography
outroVariant: b
surface: solid
tasteDials:
  DESIGN_VARIANCE: 5
  MOTION_INTENSITY: 6
  VISUAL_DENSITY: 6
colors:
  primary: "#0a0a0a"
  on-primary: "#e0e0e0"
  accent-purple: "#7c3aed"
  accent-cyan: "#06b6d4"
typography:
  headline:
    fontFamily: Inter
    fontSize: 2.5rem
    fontWeight: 200
    letterSpacing: 0.05em
  body:
    fontFamily: Inter
    fontSize: 0.875rem
    fontWeight: 300
rounded:
  sm: 4px
  md: 12px
  full: 9999px
spacing:
  sm: 16px
  md: 32px
  lg: 64px
motion:
  energy: moderate
  easing:
    entry: "sine.inOut"
    exit: "power2.out"
    ambient: "sine.inOut"
  duration:
    entrance: 1.0
    hold: 2.5
    transition: 1.5
  atmosphere:
    - particle-field
    - light-traces
    - radial-glow
  transition: gravitational-lens
differentiatorsFromLast:
  - styleName
  - openingLayout
  - outroVariant
---

# Overview

（1 段：本视频视觉气质、与口播内容的匹配理由。Agent 按所选 visual-styles.md 预设改写，勿留本示例原文。）

## Colors

- 背景 `{colors.primary}`，前景 `{colors.on-primary}`
- 强调色 `{colors.accent-purple}` / `{colors.accent-cyan}` 分工：数据用 cyan，关键词用 purple
- 禁止额外发明第四套 accent
- **禁止**未写入本文件的 lazy 色（`#3b82f6`、`#06b6d4` 等若不在 YAML 则不可用）

## Typography

- 标题 `{typography.headline}` — 竖屏 1080 宽时 headline 不小于 48px 等效
- 正文/字幕参考 **web-typography** skill 移动端层级
- **底部字幕 `.sl`**：`white-space: nowrap`；横屏单条 ≤16 汉字，竖屏 ≤12 汉字；超长拆 `subtitle` 显示层，**不拆 TTS wav**
- 禁止 Inter + 渐变字 作为唯一识别（lazy default）；读 **taste-skill** 字体纪律

## Surface（卡片材质）

- `surface: solid` — 实底 + 边框（**默认**，非 glass）
- `surface: outline` — 线框卡 + 轻 fill
- `surface: glass` — 仅当风格 preset 明确需要且 taste pre-flight 通过

## Motion Plan

（写 HTML 前必填 — 见 `anti-slop-motion-scheme.md` §三、§四 与 SKILL **P0/P1**）

- **P0 video-composition:** 每镜 8–10 元素；前景 metadata（chip/overline/分隔线）
- **P0 data-in-motion（有数据镜时）:** 列出 scN 数字镜 + 视觉载体（bar/ring/count-up）
- **P1 beat-direction（>90s 或 ≥6 镜）:** 节奏模板，如 fast-fast-SLOW-fast
- **L0 背景:** `#root` gradient + grid（静态）；可选全片 1 个慢循环
- **L1 入场:** 首镜 title stagger 0.1s；流程镜 步骤→箭头→步骤
- **L2 语义:** scN count-up @ schedule；**P1-1** 优先查 rules-index
- **L3 kinetic:** 全片 2–4 处
- **L4 转场（0.7 catalog）:** A 池 ≥3 种轮换 + 可选 1 处 B 池
- **P0-3 design-adherence:** 交付前核对 hex/字体/圆角与本文 YAML 一致

## Motion

- 入场遵循 **design-motion-principles**：stagger 0.08–0.15s，禁止每元素同一 `power2.out`
- 背景装饰：**默认静态** — `#root` gradient/网格/暗角；**禁止**每镜复制 `.ambient` + CSS `infinite`
- 可选：全片 **1 个** `#root` 级慢循环（见 `anti-slop-motion-scheme.md` L0）
- 转场：`{motion.transition}`；镜间 ≥2 种 GSAP 转场类型
- **HyperFrames only：** 仅用主时间轴 `mt`；**禁止** ScrollTrigger / scroll 监听

## Do's and Don'ts

- ✅ `#root` 静态 gradient/网格 + 镜内副信息（chip / ghost 静态词）
- ✅ 数据镜用 `{colors.accent-cyan}` 高亮数字 + count-up
- ✅ 交付前跑 **anti-slop-motion-scheme** 门禁 4 + taste pre-flight
- ❌ 左侧 accent 竖条、纯黑 `#000`、未声明的 neon cyan
- ❌ 无动效的静态 PPT 式排版
